@model OSwan_TheatreApp.Models.Post

@{
    ViewBag.Title = "ApproveDecline";
}

<h2>ApproveDecline</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>Post</h4>
    <hr />
    <table>
        <tr>
            <td>
                @Html.DisplayFor(modelItem => modelItem.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => modelItem.MainBody)
            </td>
            <td>
                @Html.DisplayFor(modelItem => modelItem.ImageUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => modelItem.DatePosted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => modelItem.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => modelItem.CategoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => modelItem.ApprovalStatus)
            </td>
        </tr>
    </table>
    <div class="form-group">
        @Html.LabelFor(model => model.ApprovalStatus, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.EnumDropDownListFor(x => x.ApprovalStatus, "Select My Type", new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.ApprovalStatus, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
